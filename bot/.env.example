# Example environment variables for the WilkesDriversUnited bot
# Copy this file to `.env` locally or set these variables in Railway / your host.

# Required
DISCORD_TOKEN=your_discord_bot_token_here

# Optional but recommended
ADMIN_TOKEN=some-secret-admin-token
# Comma-separated list of Discord user IDs that should be treated as admins
ADMIN_IDS=123456789012345678,987654321098765432

# Port for FastAPI to bind (Railway provides $PORT automatically)
PORT=8000

# Auto-timeout (seconds) before a driver is auto-removed (default 4 hours = 14400)
AUTO_TIMEOUT_SECONDS=14400

# Background purge interval in seconds (default 30 minutes = 1800)
PURGE_INTERVAL_SECONDS=1800
